{
  "name": "root",
  "private": true,
  "scripts": {
    "lint": "lerna run lint -- --fix",
    "start": "lerna run start --parallel",
    "test": "lerna run --stream --concurrency=1 test",
    "build": "lerna run build --parallel",
    "postinstall": "./node_modules/.bin/lerna link && ./node_modules/.bin/lerna bootstrap"
  },
  "devDependencies": {
    "lerna": "^3.16.4",
    "pre-commit": "^1.2.2"
  },
  "pre-commit": []
}
