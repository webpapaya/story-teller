{
  "name": "root",
  "private": true,
  "scripts": {
    "lint": "lerna run lint",
    "start": "lerna run start --parallel",
    "build": "lerna run build --parallel"
  },
  "devDependencies": {
    "lerna": "^3.16.4",
    "pre-commit": "^1.2.2"
  },
  "pre-commit": [
    "build"
  ],
  "dependencies": {
    "@storybook/addon-actions": "^5.2.4",
    "uuid": "^3.3.3"
  }
}
