# ports are prefixed with 1 (for development)
PG_PORT=15432
PG_NAME=compup_development
PG_USER=compup
PG_PASSWORD=password
# Sadly we need to specify the DATABASE_URL string here twice
DATABASE_URL=postgres://compup:password@localhost:154321/compup_test

SMTP_PORT=11025
SMTP_HOST=localhost
SMTP_HOST_USERNAME=
SMTP_HOST_PASSWORD=
MAIL_WEB_UI=11080
